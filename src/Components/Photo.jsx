import React from "react";
const Photo=(props)=>{
    return(

        <div>

        <section>

            <img src={props.picture} alt="phtoto"/>

        </section>

        </div>

    )

}
export default Photo;